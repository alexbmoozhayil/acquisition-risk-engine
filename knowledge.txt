# Acquisition Risk Engine Knowledge

- This project is a Java companion to Acquisition Intel.

- Acquisition Intel focused on RAG, search, graph, and AI workflows.

- Acquisition Risk Engine focuses on Java, Spring Boot, SQL, and risk scoring.

- Spring Boot is the Java backend framework for this project.

- Spring Boot plays a similar role to FastAPI in the Python project.

- Maven manages Java dependencies and build commands.

- The Maven wrapper, mvnw, lets the project run Maven consistently.

- spring-boot-starter-web adds REST API support and an embedded web server.

- A controller receives HTTP requests and returns HTTP responses.

- @RestController marks a Java class as a REST API controller.

- @GetMapping("/health") maps GET /health to a Java method.

- Spring Boot can convert a Java Map into a JSON response.

- The /health endpoint verifies that the service is running.





- A database migration is a versioned SQL file that changes the database schema.

- Flyway runs migration files automatically when the Spring Boot app starts.

- V1__create_initial_tables.sql is the first migration for this project.

- Flyway created the agencies, vendors, and contracts tables.

- flyway_schema_history tracks which migrations already ran.

- Spring Boot needs spring-boot-starter-flyway for Flyway integration.

- The database schema is now managed through code instead of manual SQL steps.

- PostgreSQL is running in Docker as acquisition-risk-postgres.

- The app connects to PostgreSQL using JDBC through application.properties.


I added PostgreSQL as the structured data store and used Flyway to manage schema changes through versioned SQL migrations. Instead of manually creating tables, the database schema is defined in code and automatically applied when the Spring Boot app starts. This makes the backend easier to reproduce, test, and deploy.


- V2__seed_sample_contracts.sql inserts sample contract data.

- Flyway runs V2 after V1 because migrations run in version order.

- JdbcTemplate lets Java run SQL queries against PostgreSQL.

- A repository class handles database access.

- A controller class handles HTTP requests.

- ContractResponse is a Java record used for the API response shape.

- GET /contracts reads contract rows from PostgreSQL and returns JSON.

- SQL joins combine contracts with vendor and agency names.

- We used seed data before external API ingestion to test one layer at a time.


- GET /vendors returns vendor-level summary data.

- VendorResponse is the JSON response shape for vendors.

- VendorRepository runs SQL for vendor queries.

- LEFT JOIN keeps vendors even if they have no contracts.

- COUNT counts contracts per vendor.

- SUM adds total contract value per vendor.

- COALESCE replaces null with 0.

- GROUP BY creates one summary row per vendor.

- Vendor-level summaries prepare us for vendor risk scoring.



- GET /vendors/{vendorId}/contracts returns contracts for one vendor.

- @PathVariable reads values from the URL path.

- WHERE v.id = ? filters the SQL query by vendor ID.

- JdbcTemplate passes the vendor ID as a query parameter.

- Query parameters are safer than building SQL strings manually.

- Nested endpoints are useful when one resource belongs to another resource.

- Vendor contract history is needed before calculating vendor risk.



- GET /vendors/{vendorId}/risk returns an explainable vendor risk profile.

- RiskService contains business logic for risk scoring.

- A service class sits between controllers and repositories.

- Controllers handle HTTP requests.

- Repositories handle database access.

- Services handle business rules.

- Deterministic risk rules are easier to test and explain than LLM-generated scores.

- HIGH_VALUE_CONTRACT flags vendors with contracts above a configured dollar threshold.

- MULTIPLE_CONTRACTS flags vendors with multiple contracts in the database.

- ENDED_CONTRACT flags vendors with at least one contract that already ended.

- SPARSE_DESCRIPTION flags contracts with limited description detail.

- The risk score is capped at 100.

- Risk level maps the numeric score to LOW, MEDIUM, or HIGH.

- These risk signals are review indicators, not proof that a vendor is unsafe.


- Unit tests check one piece of logic in isolation.

- RiskService can be tested without starting the server.

- RiskService can be tested without connecting to PostgreSQL.

- Mockito creates fake versions of dependencies called mocks.

- Mock repositories let tests focus on business logic instead of database access.

- when(...).thenReturn(...) defines fake repository behavior.

- assertEquals checks expected output values.

- assertThrows checks expected error behavior.

- verify checks that a mock method was called.

- verifyNoInteractions checks that a dependency was not used.

- Automated tests protect risk rules from accidental changes.

- Maven reported 4 tests because the project has 3 RiskService tests plus the generated Spring Boot context test.

I added unit tests for the risk scoring service so the business rules can be validated independently of the API and database. I used Mockito to mock the repositories, which lets the tests focus only on the scoring logic. This makes the risk engine easier to maintain because changes to scoring rules can be checked automatically with ./mvnw test.


- A refactor changes code structure without changing behavior.

- RiskRule is an interface for all risk scoring rules.

- Each risk rule class handles one specific signal.

- HighValueContractRiskRule checks for contracts above the high-value threshold.

- MultipleContractsRiskRule checks whether a vendor has more than one contract.

- EndedContractRiskRule checks whether a vendor has a contract that already ended.

- SparseDescriptionRiskRule checks whether a contract has limited description detail.

- @Component lets Spring discover a class automatically.

- @Order controls the order Spring injects rule classes into a list.

- RiskService now orchestrates rules instead of containing every rule itself.

- Separating rules makes the risk engine easier to extend and test.

I first implemented risk scoring directly inside RiskService to keep the initial version simple. After the endpoint worked and tests passed, I refactored each scoring rule into its own RiskRule class. RiskService now orchestrates a list of rules injected by Spring. This makes the design easier to extend because adding a new risk signal only requires adding a new class, not rewriting the core service.

- README.md explains what the project does and how to run it.

- A good README makes a project easier to review during interviews.

- The README should explain architecture, endpoints, setup, tests, and future work.

- The interview explanation connects the technical build to the business/domain problem.